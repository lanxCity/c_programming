#include <stdio.h>

/**
 * main - Code Entry point function
 * Return: 0 (success)
 */
int main()
{
    /**-> Variables
     * - The declaration of a variable is called "Variable Decoration"
     * - Assignment of a value to the variable declared is called "variable initialization"
     */
    int x;

    return (0);
}